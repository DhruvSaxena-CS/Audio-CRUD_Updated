<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Uploaded Audio Files</title>
  <style>
    body { font-family: Arial; padding: 2em; max-width: 700px; margin: auto; }
    .item { border-bottom: 1px solid #ccc; padding: 10px 0; }
    audio { display: block; margin-top: 5px; }
  </style>
</head>
<body>
  <h1>Uploaded Audio Files</h1>
  <a href="/">← Back to Upload Form</a>

  <% if (audios.length === 0) { %>
    <p>No audio files uploaded yet.</p>
  <% } else { %>
    <% audios.forEach(audio => { %>
      <div class="item">
        <strong><%= audio.filename %></strong><br>
        Uploaded at: <%= audio.created_at %><br>
        <audio controls src="/uploads/<%= audio.filename %>"></audio>
      </div>
    <% }) %>
  <% } %>
</body>
</html>
